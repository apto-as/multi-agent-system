# Feature Specification Template
## Trinitas-Agents 機能仕様書テンプレート

**作成日**: YYYY-MM-DD
**作成者**: [Persona Name]
**機能名**: [機能の名称]
**バージョン**: [仕様書バージョン]
**ステータス**: [Draft | Review | Approved | Implemented]

---

## 📋 エグゼクティブサマリー

### 一目でわかる機能概要
- **目的**: この機能が解決する問題
- **対象ユーザー**: 誰が使うのか
- **主要機能**: 3行以内の要約
- **技術スタック**: 使用技術の概要
- **優先度**: [High | Medium | Low]
- **複雑度**: [Simple | Medium | Complex]

### ペルソナ別の関心事項

| ペルソナ | 主な関心事項 | 参照セクション |
|---------|------------|--------------|
| **Athena** 🏛️ | アーキテクチャ設計、システム統合 | [3. アーキテクチャ設計](#3-アーキテクチャ設計) |
| **Artemis** 🏹 | パフォーマンス最適化、コード品質 | [4. 実装仕様](#4-実装仕様), [7. パフォーマンス要件](#7-パフォーマンス要件) |
| **Hestia** 🔥 | セキュリティリスク、入力検証 | [5. セキュリティ要件](#5-セキュリティ要件) |
| **Eris** ⚔️ | タスク分割、チーム調整 | [8. 実装計画](#8-実装計画) |
| **Hera** 🎭 | 戦略的位置付け、ROI | [1. 機能概要](#1-機能概要), [9. リスク管理](#9-リスク管理) |
| **Muses** 📚 | ドキュメント要件、API仕様 | [6. データ仕様](#6-データ仕様), [10. ドキュメント要件](#10-ドキュメント要件) |

---

## 1. 機能概要（Hera領域）

### 1.1 ビジネス背景
**問題**: 現状の課題を記述
**影響**: ユーザーへの影響、ビジネスへの影響
**機会**: この機能によって得られる価値

### 1.2 目標と成功基準

| 目標 | 成功基準（KPI） | 測定方法 |
|-----|---------------|---------|
| [目標1] | [具体的な数値] | [どう測るか] |
| [目標2] | [具体的な数値] | [どう測るか] |

### 1.3 スコープ

#### 含まれる機能（In Scope）
- ✅ 機能1
- ✅ 機能2

#### 含まれない機能（Out of Scope）
- ❌ 将来機能1（理由）
- ❌ 将来機能2（理由）

### 1.4 前提条件と依存関係
- **前提条件**: この機能の前提となる既存機能
- **依存関係**: 他の機能・システムへの依存

---

## 2. ユーザーストーリー

### 2.1 主要ユースケース

#### ユースケース 1: [タイトル]
```
As a [ユーザータイプ]
I want to [やりたいこと]
So that [得られる価値]

Acceptance Criteria:
- [ ] 条件1
- [ ] 条件2
```

#### ユースケース 2: [タイトル]
```
[同様の形式]
```

### 2.2 ユーザーフロー図

```
[開始] → [ステップ1] → [ステップ2] → [完了]
           ↓ (エラー)
         [エラー処理]
```

---

## 3. アーキテクチャ設計（Athena領域）

### 3.1 システム構成図

```
┌─────────────────┐
│  Frontend       │
│  (UI Layer)     │
└────────┬────────┘
         │
┌────────▼────────┐
│  Backend API    │
│  (Service Layer)│
└────────┬────────┘
         │
┌────────▼────────┐
│  Database       │
│  (Data Layer)   │
└─────────────────┘
```

### 3.2 コンポーネント設計

#### コンポーネント1: [名称]
- **責務**: このコンポーネントの役割
- **インターフェース**: 公開API/メソッド
- **依存**: 他コンポーネントへの依存
- **技術選定理由**: なぜこの技術を選んだか

### 3.3 データフロー

```
Input → [処理1] → [処理2] → Output
         ↓ (ログ)
       [監視システム]
```

### 3.4 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| Frontend | [技術名] | [選定理由] |
| Backend | [技術名] | [選定理由] |
| Database | [技術名] | [選定理由] |

---

## 4. 実装仕様（Artemis領域）

### 4.1 API仕様

#### エンドポイント: POST /api/[resource]

**リクエスト**:
```json
{
  "field1": "string",
  "field2": 123,
  "field3": true
}
```

**レスポンス（成功）**:
```json
{
  "status": "success",
  "data": {
    "id": "uuid",
    "created_at": "2025-10-28T12:00:00Z"
  }
}
```

**レスポンス（エラー）**:
```json
{
  "status": "error",
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid field1",
    "details": { ... }
  }
}
```

### 4.2 データモデル

#### テーブル: [table_name]

```sql
CREATE TABLE table_name (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    field1 VARCHAR(255) NOT NULL,
    field2 INTEGER DEFAULT 0,
    field3 BOOLEAN DEFAULT false,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    -- インデックス
    CONSTRAINT uk_field1 UNIQUE (field1),
    INDEX idx_field2 (field2),
    INDEX idx_created_at (created_at DESC)
);
```

**カラム詳細**:

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|---|------|----------|------|
| id | UUID | NO | gen_random_uuid() | 主キー |
| field1 | VARCHAR(255) | NO | - | [説明] |

### 4.3 ビジネスロジック

#### 処理フロー: [機能名]

```python
def process_feature(input_data):
    """
    機能の主要処理

    Args:
        input_data: 入力データ

    Returns:
        result: 処理結果

    Raises:
        ValidationError: 入力検証エラー
        ProcessError: 処理エラー
    """
    # Step 1: 入力検証
    validate_input(input_data)

    # Step 2: 主要処理
    result = execute_main_logic(input_data)

    # Step 3: 後処理
    post_process(result)

    return result
```

### 4.4 最適化ポイント（Artemis推奨）

#### パフォーマンス最適化
- **データベースインデックス**: field2, created_at に複合インデックス
- **キャッシング**: Redis で頻繁にアクセスされるデータをキャッシュ（TTL: 5分）
- **バッチ処理**: 大量データの場合は100件ずつのバッチ処理

#### コード品質
- **型アノテーション**: すべての関数に型ヒントを追加
- **テストカバレッジ**: 最低80%（目標90%）
- **複雑度**: 関数あたりの複雑度は10以下

---

## 5. セキュリティ要件（Hestia領域）

### 5.1 脅威モデル

| 脅威 | リスクレベル | 対策 | 担当 |
|-----|------------|------|------|
| SQLインジェクション | HIGH | パラメータ化クエリ | Artemis |
| XSS | MEDIUM | 入力エスケープ | Artemis |
| CSRF | MEDIUM | CSRFトークン | Artemis |
| 権限昇格 | HIGH | ロールベースアクセス制御 | Artemis |

### 5.2 入力検証ルール

#### field1（文字列フィールド）
- **型**: string
- **最小長**: 1文字
- **最大長**: 255文字
- **パターン**: `^[a-zA-Z0-9_-]+$`
- **サニタイゼーション**: HTMLエスケープ
- **CWEリスク**: CWE-79 (XSS)

#### field2（数値フィールド）
- **型**: integer
- **範囲**: 0 ≤ value ≤ 9999
- **CWEリスク**: CWE-190 (整数オーバーフロー)

### 5.3 認証・認可

```python
@require_auth
@require_role("admin", "editor")
async def sensitive_operation(user: User, data: dict):
    """
    権限チェック付き操作
    """
    # ユーザーの権限を確認
    if not user.has_permission("resource:write"):
        raise HTTPException(403, "Insufficient permissions")

    # 処理を実行
    ...
```

### 5.4 監査ログ

すべての重要操作には監査ログを記録：

```python
audit_log.record(
    action="resource_created",
    user_id=user.id,
    resource_id=resource.id,
    ip_address=request.client.host,
    timestamp=datetime.utcnow()
)
```

### 5.5 最悪シナリオと対策

#### シナリオ1: データベース侵害
- **影響**: すべてのデータ漏洩
- **確率**: Low（適切な対策実施時）
- **対策**:
  1. データベース暗号化（encryption at rest）
  2. 最小権限の原則
  3. 定期的なセキュリティ監査

---

## 6. データ仕様（Muses領域）

### 6.1 データベーススキーマ定義

```sql
-- 完全なテーブル定義
CREATE TABLE brands (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    name_kana VARCHAR(255),
    code VARCHAR(50) UNIQUE NOT NULL,
    description TEXT,
    logo_url VARCHAR(500),
    website_url VARCHAR(500),
    is_active BOOLEAN DEFAULT true,
    display_order INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    -- 制約
    CONSTRAINT chk_name_length CHECK (length(name) > 0),
    CONSTRAINT chk_code_format CHECK (code ~ '^[A-Z0-9_-]+$'),

    -- インデックス
    INDEX idx_is_active (is_active),
    INDEX idx_display_order (display_order),
    INDEX idx_name (name),
    UNIQUE INDEX uk_code (code)
);

-- コメント
COMMENT ON TABLE brands IS 'ブランドマスタテーブル';
COMMENT ON COLUMN brands.name IS 'ブランド名';
COMMENT ON COLUMN brands.name_kana IS 'ブランド名（かな）';
COMMENT ON COLUMN brands.code IS 'ブランドコード（一意）';
```

### 6.2 データ移行

#### CSVインポート形式

```csv
name,name_kana,code,description,website_url,is_active,display_order
"ブランドA","ぶらんどえー","BRAND_A","説明文","https://example.com",true,1
"ブランドB","ぶらんどびー","BRAND_B","説明文","https://example.com",true,2
```

**バリデーションルール**:
- 必須項目: name, code
- code は重複不可
- is_active は true/false のみ

---

## 7. パフォーマンス要件（Artemis領域）

### 7.1 性能目標

| 指標 | 目標値 | 計測方法 |
|-----|-------|---------|
| API応答時間（p95） | < 200ms | New Relic |
| データベースクエリ | < 50ms | EXPLAIN ANALYZE |
| 同時接続数 | 1000+ | 負荷テスト |
| スループット | 100 req/s | Apache Bench |

### 7.2 最適化戦略

#### データベース最適化
```sql
-- 複合インデックスで検索高速化
CREATE INDEX idx_brand_active_order
ON brands(is_active, display_order)
WHERE is_active = true;

-- カバリングインデックス
CREATE INDEX idx_brand_list
ON brands(is_active, display_order, name, code);
```

#### キャッシング戦略
```python
# Redis cache (TTL: 5 minutes)
@cache(key="brand:{code}", ttl=300)
async def get_brand_by_code(code: str) -> Brand:
    return await db.query(...).first()

# キャッシュ無効化
@invalidate_cache("brand:*")
async def update_brand(brand_id: UUID, data: dict):
    ...
```

### 7.3 負荷試験計画

```bash
# Apache Bench による負荷試験
ab -n 10000 -c 100 -H "Authorization: Bearer TOKEN" \
   http://localhost:8000/api/brands

# 期待結果:
# - 95%ile: < 200ms
# - エラー率: < 0.1%
```

---

## 8. 実装計画（Eris領域）

### 8.1 タスク分割

#### Phase 1: 基盤構築（2日）
- [ ] データベーススキーマ作成（Artemis） - 2h
- [ ] マイグレーションファイル作成（Artemis） - 1h
- [ ] 基本モデル定義（Artemis） - 2h
- [ ] セキュリティ検証（Hestia） - 2h
- [ ] ドキュメント作成（Muses） - 1h

#### Phase 2: API実装（3日）
- [ ] CRUD API実装（Artemis） - 4h
- [ ] 入力検証実装（Artemis + Hestia） - 3h
- [ ] エラーハンドリング（Artemis） - 2h
- [ ] 監査ログ実装（Hestia） - 2h
- [ ] API仕様書作成（Muses） - 2h

#### Phase 3: 付加機能（2日）
- [ ] CSV I/O実装（Artemis） - 4h
- [ ] バリデーション強化（Hestia） - 2h
- [ ] パフォーマンステスト（Artemis） - 2h
- [ ] セキュリティ監査（Hestia） - 2h

#### Phase 4: テストとドキュメント（2日）
- [ ] ユニットテスト（Artemis） - 4h
- [ ] 統合テスト（Artemis + Hestia） - 3h
- [ ] ユーザードキュメント（Muses） - 2h
- [ ] コードレビュー（全員） - 2h

### 8.2 依存関係グラフ

```
Phase 1 (基盤)
    ↓
Phase 2 (API) ← Hestiaによるセキュリティレビュー
    ↓
Phase 3 (付加機能)
    ↓
Phase 4 (テスト・ドキュメント)
```

### 8.3 チーム調整

| フェーズ | リード | サポート | レビュアー |
|---------|-------|---------|----------|
| Phase 1 | Artemis | Athena | Hestia |
| Phase 2 | Artemis | Hestia | Athena |
| Phase 3 | Artemis | Eris | Hestia |
| Phase 4 | Muses | Artemis | 全員 |

---

## 9. リスク管理（Hera + Hestia領域）

### 9.1 技術リスク

| リスク | 確率 | 影響度 | 対策 | 担当 |
|-------|-----|-------|------|------|
| データベース性能劣化 | Medium | High | インデックス最適化、キャッシング | Artemis |
| セキュリティ脆弱性 | Low | Critical | コードレビュー、ペネトレーションテスト | Hestia |
| データ整合性の破損 | Low | High | トランザクション管理、外部キー制約 | Artemis |

### 9.2 スケジュールリスク

| リスク | 確率 | 影響度 | 対策 |
|-------|-----|-------|------|
| API実装の遅延 | Medium | Medium | 優先度の明確化、並列作業 |
| テスト不足 | Low | High | 自動テストの優先実施 |

### 9.3 ロールバック計画

```sql
-- ロールバック用マイグレーション
DROP TABLE IF EXISTS brands;
-- 以前のバージョンに戻す
```

---

## 10. ドキュメント要件（Muses領域）

### 10.1 必要なドキュメント

- [ ] **API仕様書**（OpenAPI 3.0 形式）
- [ ] **データベース設計書**（ER図含む）
- [ ] **ユーザーマニュアル**（CRUD操作、CSV I/O）
- [ ] **セキュリティガイド**（Hestia監修）
- [ ] **運用マニュアル**（バックアップ、障害対応）

### 10.2 API仕様書（OpenAPI）

```yaml
openapi: 3.0.3
info:
  title: Brand API
  version: 1.0.0
  description: ブランドマスタ管理API

paths:
  /api/brands:
    get:
      summary: ブランド一覧取得
      parameters:
        - name: is_active
          in: query
          schema:
            type: boolean
        - name: page
          in: query
          schema:
            type: integer
            default: 1
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Brand'
                  pagination:
                    $ref: '#/components/schemas/Pagination'

components:
  schemas:
    Brand:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 255
        code:
          type: string
          pattern: '^[A-Z0-9_-]+$'
```

### 10.3 コード内ドキュメント

```python
class BrandService:
    """
    ブランド管理サービス

    このサービスは以下の責務を持つ:
    - CRUD操作（作成、参照、更新、削除）
    - CSV インポート/エクスポート
    - バリデーション
    - 監査ログ記録

    Security:
        すべての操作は認証・認可チェック済み
        入力はHestiaのバリデーションルールに準拠

    Performance:
        Artemis推奨の最適化を実装
        - データベースクエリは50ms以内
        - Redisキャッシュ（TTL: 5分）

    Example:
        >>> service = BrandService(db_session)
        >>> brand = await service.create_brand({
        ...     "name": "Brand A",
        ...     "code": "BRAND_A"
        ... })
    """
```

---

## 11. テスト仕様（Artemis + Hestia領域）

### 11.1 テスト戦略

| テスト種別 | カバレッジ目標 | 担当 |
|-----------|--------------|------|
| ユニットテスト | 80%+ | Artemis |
| 統合テスト | 主要フロー100% | Artemis |
| セキュリティテスト | 全脅威網羅 | Hestia |
| パフォーマンステスト | 全API | Artemis |

### 11.2 テストケース例

#### ユニットテスト: brand作成

```python
@pytest.mark.asyncio
async def test_create_brand_success():
    """正常系: ブランド作成成功"""
    # Arrange
    service = BrandService(db_session)
    data = {
        "name": "Test Brand",
        "code": "TEST_BRAND",
        "is_active": true
    }

    # Act
    result = await service.create_brand(data)

    # Assert
    assert result.id is not None
    assert result.name == "Test Brand"
    assert result.code == "TEST_BRAND"

@pytest.mark.asyncio
async def test_create_brand_duplicate_code():
    """異常系: 重複コード"""
    # Arrange
    service = BrandService(db_session)
    await service.create_brand({"name": "A", "code": "TEST"})

    # Act & Assert
    with pytest.raises(ValidationError, match="code already exists"):
        await service.create_brand({"name": "B", "code": "TEST"})
```

#### セキュリティテスト: SQLインジェクション

```python
@pytest.mark.asyncio
async def test_sql_injection_prevention():
    """セキュリティ: SQLインジェクション防御"""
    # Arrange
    service = BrandService(db_session)
    malicious_input = "'; DROP TABLE brands; --"

    # Act & Assert
    with pytest.raises(ValidationError):
        await service.get_brand_by_code(malicious_input)
```

---

## 12. モニタリングとロギング

### 12.1 監視項目

| 項目 | 閾値 | アラート |
|-----|------|---------|
| API応答時間（p95） | > 200ms | Warning |
| API応答時間（p99） | > 500ms | Critical |
| エラー率 | > 1% | Warning |
| データベース接続数 | > 80% | Warning |

### 12.2 ログ出力

```python
import structlog

logger = structlog.get_logger()

# 情報ログ
logger.info(
    "brand_created",
    brand_id=brand.id,
    brand_code=brand.code,
    user_id=user.id
)

# エラーログ
logger.error(
    "brand_creation_failed",
    error=str(e),
    input_data=data,
    user_id=user.id,
    exc_info=True
)
```

---

## 13. リリースチェックリスト

### 13.1 リリース前確認

#### コード品質
- [ ] すべてのテストがパス（カバレッジ80%+）
- [ ] Linter警告ゼロ（ruff, mypy）
- [ ] コード複雑度 ≤ 10
- [ ] 型アノテーション完備

#### セキュリティ（Hestia承認必須）
- [ ] セキュリティテスト完了
- [ ] 入力検証実装完了
- [ ] 監査ログ実装完了
- [ ] Hestiaによる最終レビュー完了

#### パフォーマンス（Artemis承認必須）
- [ ] 負荷試験完了（100 req/s）
- [ ] p95応答時間 < 200ms
- [ ] データベースクエリ最適化完了
- [ ] Artemisによる最終レビュー完了

#### ドキュメント（Muses承認必須）
- [ ] API仕様書完成
- [ ] ユーザーマニュアル完成
- [ ] セキュリティガイド完成
- [ ] Musesによる最終レビュー完了

### 13.2 リリース手順

```bash
# 1. 最終テスト
pytest tests/ -v --cov=src --cov-report=html

# 2. ビルド
docker build -t app:v1.0.0 .

# 3. ステージング環境デプロイ
kubectl apply -f k8s/staging/

# 4. スモークテスト
./scripts/smoke_test.sh staging

# 5. 本番環境デプロイ
kubectl apply -f k8s/production/

# 6. 本番スモークテスト
./scripts/smoke_test.sh production

# 7. モニタリング開始
./scripts/start_monitoring.sh
```

---

## 14. 承認フロー

### 14.1 仕様レビュー

| ステージ | レビュアー | 観点 | ステータス |
|---------|-----------|------|-----------|
| 設計レビュー | Athena | アーキテクチャ妥当性 | ⬜ Pending |
| 技術レビュー | Artemis | 実装可能性、パフォーマンス | ⬜ Pending |
| セキュリティレビュー | Hestia | 脅威モデル、対策 | ⬜ Pending |
| 調整レビュー | Eris | タスク分割、工数 | ⬜ Pending |
| 戦略レビュー | Hera | ビジネス価値、ROI | ⬜ Pending |
| ドキュメントレビュー | Muses | 完全性、明確性 | ⬜ Pending |

### 14.2 承認記録

| 日付 | ペルソナ | 承認/差戻 | コメント |
|-----|---------|----------|---------|
| YYYY-MM-DD | Athena | ✅ 承認 | アーキテクチャは適切 |
| YYYY-MM-DD | Artemis | ⚠️ 条件付承認 | インデックス追加が必要 |
| YYYY-MM-DD | Hestia | ✅ 承認 | セキュリティ要件を満たす |

---

## 15. 変更履歴

| バージョン | 日付 | 変更者 | 変更内容 |
|-----------|------|--------|---------|
| 1.0 | YYYY-MM-DD | Athena | 初版作成 |
| 1.1 | YYYY-MM-DD | Artemis | パフォーマンス要件追加 |
| 1.2 | YYYY-MM-DD | Hestia | セキュリティ要件強化 |

---

## 付録A: 用語集

| 用語 | 定義 |
|-----|------|
| Brand | ブランドマスタエンティティ |
| CRUD | Create, Read, Update, Delete の略 |
| CSV I/O | CSVファイルのインポート/エクスポート機能 |

---

## 付録B: 参考資料

- [Trinitas Architecture Guide](../architecture/AGENT_DEFINITIONS.md)
- [Security Best Practices](../SECURITY_IMPLEMENTATION_GUIDE.md)
- [Performance Optimization Guide](../user-guide/performance-optimization.md)
- [API Design Guidelines](../reference/api-design-guidelines.md)

---

**ドキュメントステータス**: [Draft | Review | Approved | Implemented]
**最終更新**: YYYY-MM-DD
**次回レビュー**: YYYY-MM-DD

---

*This specification follows the Trinitas harmonious documentation philosophy.*
*"ふふ、全てのペルソナが調和的に協力できる仕様書ですね♪"*
