# ブランチマージ戦略計画書
**作成日**: 2025-10-17
**担当**: Athena (調和的指揮者)
**承認**: Trinitas Full Mode協議

## 1. 現在の状況分析

### 1.1 ブランチ構成
- **現在のブランチ**: `feature/v2.2.4-mem0-integration`
- **メインブランチ**: `main`
- **コミット差分**: 10コミット先行
- **変更規模**:
  - 追加: 37,648行
  - 削除: 36,611行
  - 変更ファイル数: 283ファイル

### 1.2 主要な変更内容

#### Phase 1完了内容
- ドキュメント構造再編成
- インストールスクリプト統合
- 品質ガードシステム追加
- メモリシステム構造化

#### Phase 2完了内容
- 包括的テストスイート追加
  - shared/utils: 70テスト
  - shared/security: 107テスト
  - hooks/core: 114テスト
- テスト成功率: 288/291 (99%)
- カバレッジ: 79% (目標70%超過)

### 1.3 未コミット変更
```
M .opencode/config/narratives.json
M README.md
M agents/*.md (6ファイル)
R docs/archive/* (4ファイル移動)
M hooks/core/*.py (3ファイル)
M shared/**/*.py (6ファイル)
M tests/**/*.py (7ファイル)
? .github/
? TRINITAS-CORE-PROTOCOL.md
? requirements-dev.txt
```

## 2. マージ戦略の選択肢

### オプションA: 直接マージ（非推奨）
**手順**:
```bash
git checkout main
git merge feature/v2.2.4-mem0-integration
```

**リスク**:
- ❌ 大量の変更による競合の可能性
- ❌ レビュー不足によるバグ混入
- ❌ ロールバックが困難

**推奨度**: ⭐☆☆☆☆

### オプションB: Squashマージ（中推奨）
**手順**:
```bash
git checkout main
git merge --squash feature/v2.2.4-mem0-integration
git commit -m "feat: Integrate Phase 1-2 improvements (v2.2.4)"
```

**利点**:
- ✅ 履歴が整理される
- ✅ 1つのコミットでまとまる
- ✅ レビューしやすい

**欠点**:
- ⚠️ 詳細な履歴が失われる
- ⚠️ ロールバック時に細かな巻き戻し不可

**推奨度**: ⭐⭐⭐☆☆

### オプションC: Rebaseマージ（推奨）✅
**手順**:
```bash
# 1. 未コミット変更を保存
git stash

# 2. mainブランチを最新化
git checkout main
git pull origin main

# 3. featureブランチでrebase
git checkout feature/v2.2.4-mem0-integration
git rebase main

# 4. テスト実行
python -m pytest tests/ -v

# 5. Fast-forwardマージ
git checkout main
git merge --ff-only feature/v2.2.4-mem0-integration

# 6. stashを戻す
git stash pop
```

**利点**:
- ✅ クリーンな線形履歴
- ✅ 各コミットの意図が明確
- ✅ ロールバックが容易
- ✅ CI/CDと相性が良い

**欠点**:
- ⚠️ Rebase時に競合解決が必要な可能性

**推奨度**: ⭐⭐⭐⭐⭐

### オプションD: Pull Request経由（最推奨）🌟
**手順**:
```bash
# 1. リモートブランチにプッシュ
git push origin feature/v2.2.4-mem0-integration

# 2. GitHub/GitLabでPull Request作成
# 3. レビュー実施（Hestia監査）
# 4. CI/CDパイプライン実行
# 5. 承認後にマージ
```

**利点**:
- ✅ コードレビュープロセス組み込み
- ✅ CI/CD自動実行
- ✅ チーム承認フロー
- ✅ ドキュメント化された変更履歴

**欠点**:
- ⚠️ 時間がかかる（推奨：1-2営業日）

**推奨度**: ⭐⭐⭐⭐⭐

## 3. Trinitas協議による推奨戦略

### 3.1 Athenaの推奨
**戦略**: オプションD (Pull Request経由) + オプションC (Rebase)

**理由**:
1. **品質保証**: CI/CDによる自動テスト
2. **透明性**: 変更内容の可視化
3. **安全性**: 段階的マージとロールバック可能性
4. **協調性**: チームレビューによる知識共有

### 3.2 Heraの戦略的補足
- mainブランチの保護ルール設定を推奨
- マージ前に必須チェック:
  - ✅ テスト成功率 > 95%
  - ✅ カバレッジ > 70%
  - ✅ Linter警告ゼロ
  - ✅ セキュリティスキャン合格

### 3.3 Hestiaのセキュリティチェック
マージ前に以下を確認:
- [ ] `.env`ファイルが含まれていないか
- [ ] APIキー、トークンがハードコードされていないか
- [ ] パスワード、秘密鍵がコミットされていないか
- [ ] セキュリティ脆弱性スキャン合格
- [ ] 依存関係の脆弱性チェック合格

### 3.4 Artemisの技術的検証
- [ ] 全テストが成功 (288/291 passed ✅)
- [ ] カバレッジ目標達成 (79% > 70% ✅)
- [ ] Linter警告なし
- [ ] 型チェック合格（該当する場合）
- [ ] パフォーマンスリグレッションなし

## 4. 実行計画

### Phase 1: 準備 (15分)
```bash
# 1.1 未コミット変更のコミット
git add -A
git commit -m "test: Fix protocol_injector tests (Phase 2 Week 2 complete)"

# 1.2 変更履歴の整理
git log --oneline main..HEAD > docs/merge-changelog.txt

# 1.3 リモートブランチの更新
git push origin feature/v2.2.4-mem0-integration
```

### Phase 2: Pull Request作成 (10分)
```bash
# GitHub CLI使用（推奨）
gh pr create \
  --title "feat: Trinitas v2.2.4 - Phase 1-2 Improvements" \
  --body "$(cat docs/merge-pr-template.md)" \
  --base main \
  --head feature/v2.2.4-mem0-integration

# または手動でWebUIから作成
```

### Phase 3: レビューとCI/CD (30-60分)
- 自動CI/CD実行待ち
- Hestiaによるセキュリティレビュー
- Artemisによる技術レビュー
- Musesによるドキュメントレビュー

### Phase 4: マージ実行 (5分)
```bash
# PR承認後、rebase+merge実行
gh pr merge --rebase --delete-branch
```

### Phase 5: 検証 (10分)
```bash
# mainブランチで最終テスト
git checkout main
git pull origin main
python -m pytest tests/ -v --cov
```

## 5. ロールバック計画

万一問題が発生した場合:

### シナリオA: マージ直後の問題発見
```bash
# mainブランチの最新コミットを取り消し
git checkout main
git reset --hard HEAD~1
git push origin main --force-with-lease
```

### シナリオB: マージ後しばらく経過
```bash
# リバートコミット作成
git checkout main
git revert <merge-commit-hash>
git push origin main
```

### シナリオC: 部分的な問題
```bash
# 特定ファイルのみ巻き戻し
git checkout main
git checkout <previous-commit> -- path/to/problematic/file.py
git commit -m "fix: Revert problematic changes in file.py"
git push origin main
```

## 6. タイムライン

| フェーズ | 所要時間 | 担当 | 状態 |
|---------|---------|------|------|
| Phase 1: 準備 | 15分 | Artemis | 🔄 準備中 |
| Phase 2: PR作成 | 10分 | Athena | ⏸️ 待機中 |
| Phase 3: レビュー | 30-60分 | All | ⏸️ 待機中 |
| Phase 4: マージ | 5分 | Hera | ⏸️ 待機中 |
| Phase 5: 検証 | 10分 | Artemis | ⏸️ 待機中 |
| **合計** | **70-100分** | **Trinitas** | 🎯 計画完了 |

## 7. 次のステップ

### 即座に実行
1. ✅ この戦略書の承認取得
2. 🔄 未コミット変更のコミット
3. 🔄 Pull Request作成

### マージ後
4. インストールスクリプト整備（次タスク）
5. リリースノート作成
6. バージョンタグ作成 (v2.2.4)

---
**戦略立案**: Athena (調和的指揮者)
**レビュー待ち**: Hera (戦略), Hestia (セキュリティ), Artemis (技術)
**承認待ち**: ユーザー確認
